import { add, Circle } from './math';
import { unusedThing } from './math';

const n = add(1, 2);
const c = new Circle(3);
const wrong: number = 'str';

export function main(): void {
  console.log(Math.);
  c.area();
}

main();

console.lg(n);